# tuneport spicetify bridge

copy `tuneport.js` into your spicetify `Extensions` folder, then run:

```bash
spicetify apply
```

set your bridge token via devtools console:

```js
localStorage.setItem('tuneport_bridge_token', '<token>')
```
